[build]
builder = "NIXPACKS"
srcDir = "apps/server"
buildCommand = "npm install -g pnpm@9 && pnpm install --frozen-lockfile && pnpm prisma generate && pnpm build"
installCommand = "npm install -g pnpm@9 && pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm start"
