[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @massage-bot/server prisma generate && pnpm --filter @massage-bot/server build"
installCommand = "pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm --filter @massage-bot/server start"
